import React from "react";

// Usar un array con las cláusulas (tal vez cada clausula es un objeto con propiedades como "nombre, nùmero, contenido, etc")
// Usar metodos de array **inmutables** (filter, map, slice) para mantener el orden de las clausulas 

const clausulas = [{}, {}, {}];

clausulas.filter((clausula, index) => {});
